export enum GenderType {
  MALE = "MALE",
  FEMALE = "FEMALE",
}

export enum ExamRegistrationStatusType {
  REGISTERED = 'REGISTERED',
  BLOCKED = 'BLOCKED',
  BANNED = 'BANNED',
  CANCELLED = 'CANCELLED'
}